{
  "name": "@meterr/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared configuration for meterr.ai",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
